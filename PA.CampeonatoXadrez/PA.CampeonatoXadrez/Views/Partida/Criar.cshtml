@*@model PA.CampeonatoXadrez.ViewModel.PartidaViewModel
@{
    ViewBag.Title = "Criar";
}

<h2>Criar</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>Disciplina</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group">
            <div class="col-md-10">
                @Html.DropDownListFor(p => p.JogadorId1, new SelectList(@Model.ListaJogador, "Id", "nome"), new { @class = "form-control" })
            </div>
        </div>
        @*<div class="form-group">
            <div class="col-md-10">
                @Html.DropDownListFor(p => p.JogadorId2, new SelectList(@Model.ListaJogador, "JogadorId2", "nomeJogador2"), new { @class = "form-control" })
            </div>
        </div>*@
        @*<div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
}*@

